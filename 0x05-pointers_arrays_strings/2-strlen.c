#include "main.h"

/**
 * _strlen - a function that return the length of a string
 * @s - char to be checked
 *
 * Return: int
*/
int _strlen(char *s)
{
	int b;
	for (b = 0; s[b] != '\0'; b++)
		return (b);
}

