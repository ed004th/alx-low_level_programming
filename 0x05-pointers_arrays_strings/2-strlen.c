#include "main.h"

/**
 * _strlen - a function that return the length of a string
 * @s: int to be checked
 *
 * Return: 0
*/
int _strlen(char *s)
{
	int b;

	for (b = 0; s[b] != '\0'; b++)
		return (b);
}

