#include "main.h"
/**
 * _puts - a function that prints a string followed by a newline
 * @str - char to checked
 *
*/
void _puts(char *str)
{
	int index
		for (index = 0; str[index] != '\0'; index++)
		{
			_putchar (str[index]);
		}
	_putchar ('\n');
}
